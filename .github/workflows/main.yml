# GitHub Action for generating a contribution graph with a snake eating your contributions.
name: Generate Snake and Stats

# Controls when the action will run.
on:
  schedule:
      # every 12 hours
    - cron: "0 */12 * * *"

  # This command allows us to run the Action automatically from the Actions tab.
  workflow_dispatch:
 
  # Also run on every push on the main branch
  push:
    branches:
    - main

# The job pushes to the "output" branch, so the token needs write access
# (new repos default GITHUB_TOKEN to read-only).
permissions:
  contents: write

# The sequence of runs in this workflow:
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      - name: Clone repo
        uses: actions/checkout@v4
   
      # The public github-readme-stats.vercel.app instance is rate-limited and
      # often returns errors, so build the stats cards here instead.
      # These run before the snake step: snk runs in a root-owned Docker
      # container, and if it creates ./dist first this action can't write to it.
      - name: Create output folder
        run: mkdir -p dist

      - name: Generate stats card
        uses: stats-organization/github-readme-stats-action@v2
        with:
          card: stats
          options: username=t3rminal-cmd&show_icons=true&locale=en
          path: dist/stats.svg
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate top languages card
        uses: stats-organization/github-readme-stats-action@v2
        with:
          card: top-langs
          options: username=t3rminal-cmd&layout=compact&locale=en
          path: dist/top-langs.svg
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate the snake files in './dist/'
        uses: Platane/snk@v3
        id: snake-gif
        with:
          github_user_name: t3rminal-cmd
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark
            dist/github-contribution-grid-snake.gif?color_snake=orange&color_dots=#bfd6f6,#8dbdff,#64a1f4,#4b91f1,#3c7dd9
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Show build status
        run: git status

      - name: Push new files to the output branch
        uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output
          build_dir: dist
          commit_message: Update snake animations and stats cards
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
